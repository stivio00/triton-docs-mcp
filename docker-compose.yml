services:
  triton-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${TRITON_MCP_PORT:-8080}:8080"
    environment:
      - TRITON_MCP_PORT=8080
      - TRITON_MCP_HOST=0.0.0.0
    volumes:
      - triton-index:/root/.triton_mcp_index
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8080/mcp', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  triton-index: